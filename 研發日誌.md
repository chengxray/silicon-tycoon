# 《Silicon Tycoon: Foundry Master（矽島霸權）》研發日誌 (Development Log)

**記錄日期**：2026-09-07 ~ 2026-09-08  
**專案版本**：v0.2.0 (Alpha Prototype)  
**研發負責**：Antigravity Pair Programming Team  

---

## 📌 今日工作總覽

今日聚焦於《矽島霸權》的**底層科學引擎建立、2.5D 卡通化美術素材庫整合、2.5D 等角無塵室渲染場景、以及三大營運互動中樞（合約板、商城、人資）之全面實作**。所有數學模型均通過自動化單元測試，TypeScript 靜態檢查零錯誤，基礎遊戲循環流暢運轉。

---

## 🛠️ 今日具體完成模組清單

### 一、 專案架構與建置基底 (Architecture & Build Setup)
1. **靜態部署與編譯優化**：
   - 設定 `Vite + TypeScript` 專案環境，配置 `base: './'`，支援 GitHub Pages 純靜態部署。
   - 解決 Phaser 3 巨大 AST 在 Windows 環境的編譯耗時問題：採用 `public/phaser.min.js` 靜態部署，TypeScript 透過 `import type * as PhaserTypes` 實現型別安全與自動補全，大幅提升開發與打包效率。
2. **純邏輯與視圖完全解耦**：
   - 模擬運算全數收斂於 `src/engine/`，不依賴 Phaser 畫面生命週期，支援後續無痛移植或 Headless 測試。

---

### 二、 核心科學與模擬運算引擎 (Core Simulation Engines)
今日完成 9 大純邏輯運算引擎，並經 `test_engines.ts` 自動化測試 100% 通過：

1. **微影光學與動態 $k_1$ 引擎 (`src/engine/RayleighEngine.ts`)**：
   - 嚴格實作 Rayleigh 極限解析度公式 $CD = k_1 \cdot \frac{\lambda}{NA}$。
   - 涵蓋 10µm Contact Aligner (3007nm) 到 2nm High-NA EUV (20nm) 全光學規格庫。
   - 實作**動態 $k_1$ 因子模型**：$k_1 = k_{1,\text{tech}} + \Delta k_{1,\text{wear}} - \Delta k_{1,\text{engineer}} + \Delta k_{1,\text{fatigue}}$。
   - 極限制程視窗（$k_1 < 0.38$）之聚焦景深良率懲罰計算。
2. **經濟、報價與債務保護引擎 (`src/engine/EconomyEngine.ts`)**：
   - 10µm 至 2nm BasePrice 與 BaseNRE 官方定價表。
   - **NRE 光罩款秒到帳機制**：接單當下客戶即刻預付全額光罩研發費，提供前期資金運轉。
   - 客戶信任乘數 `TrustMultiplier`：連動 5 筆滑動良率歷史（0.75x ~ 1.25x 浮動）。
   - 8h/24h 逾期階梯式追討與**薪資水電絕對法律優先權**（防惡性破產死亡螺旋）。
3. **晶圓良率與缺陷擴散引擎 (`src/engine/YieldEngine.ts`)**：
   - 多層微影蝕刻良率連乘積計算。
   - **5 筆歷史滑動窗口良率指數 (`RollingYieldIndex`)**：報廢強制記為 0%。
   - 蒙地卡羅 25 晶粒圖產生器：同心圓良率梯度（中心 1.1x / 邊緣 0.7x）與隨機群聚缺陷 (Cluster Defects)。
4. **預防保養、班別與炸機風險引擎 (`src/engine/MaintenanceEngine.ts`)**：
   - 機台磨損累積與 MTTR 修復時間計算。
   - **🛡️ TPM 24H 零故障在線維護保證**：專長符合 + 職等合規 + 三班制疲勞 < 50 時，機台磨損鎖定 $\le 5\%$，故障率精準為 0%。
   - 越級操作 $\ge 2$ 級觸發 25% 炸機風險判定。
5. **Option B 生產流動狀態機 (`src/engine/ProductionEngine.ts`)**：
   - 站點流動：`FILM → LIT (COAT → EXPOSE → DEVELOP) → ETCH → DIFF [→ CMP]`。
   - 30s/45s/35s Q-Time 死線實時監聽（輕微逾時降良率 35%，嚴重逾時觸發光阻重洗 Rework 機制）。
   - 工廠負荷百分比 ($W_{\%}$) 與 🔴 紅色驚嘆號智能瓶頸逆向診斷歸因。
6. **自適應任務引擎 (`src/engine/QuestEngine.ts`)**：
   - 每日 00:00 依 Foundry Tier 動態生成 3 項貼合進度的營運任務。
   - 3/3 每日全勤特獎與每週 15 任務龍頭週大獎。
7. **半導體成就系統 (`src/engine/AchievementEngine.ts`)**：
   - 16 項成就（新手入門、製程突破、廠務卓越、品質良率）狀態監聽與現金領取。
8. **存檔與離線蒙地卡羅引擎 (`src/services/SaveGameService.ts`)**：
   - `SaveGameV2` 規格定義、V1 平滑升級遷移機制。
   - LocalStorage 10 秒自動存檔與 JSON 匯出/匯入備份。
   - 4 小時離線掛機模擬（符合 TPM 者享有 100% 免維修全額出貨結算）。
9. **程序化音效系統 (`src/audio/SoundEffects.ts`)**：
   - 原生 Web Audio API 合成按鈕點擊聲、金幣入帳音、警笛聲、炸機聲、通關號角與光阻清洗音，零外部檔案依賴。

---

### 三、 美術資產與素材庫升級 (Assets & Registry 2.5D Upgrade)
1. **全面消滅簡陋佔位 SVG，升級為精美 2.5D 等角立體資產**：
   - 利用重置後之生成配額，補齊全廠所有先前缺乏獨立繪圖之設備與載具，全套皆為**無廠牌標誌（No-Brand）、純白去背、嚴格 2.5D 等角 30° 透視之高品質 Tycoon 遊戲貼圖**：
     - `litho_projection.jpg`：1x 反射鏡等倍投影曝光機（Perkin-Elmer 經典反射光學系統）
     - `litho_arfdry.jpg`：ArF Dry 193nm 乾式微影掃描機（光學腔體、雙工件台）
     - `track_manual.jpg`：1970 經典黃光手動旋塗熱板台（旋塗吸盤、注射臂、150°C 溫控熱板）
     - `track_dual.jpg`：雙軌連線 Track（旋塗塗膠室與顯影室實體隔離、雙機械手臂、輸送軌道）
     - `track_advanced.jpg`：先進極限分子級 EUV Track（雙真空 FOUP 艙口、分子級 MOR 塗布、直立烘烤塔）
     - `etch_wet.jpg`：濕式化學酸槽清洗台（H2SO4、HNO3、QDR 快速沖洗槽、機械浸泡手臂、抽氣管道）
     - `film_pecvd.jpg`：PECVD / ALD 電漿薄膜生長反應腔（圓形視窗紫色電漿輝光、RF 射頻盒、多色氣體歧管）
     - `diff_implanter.jpg`：大束流離子佈植機（巨大弧形電磁偏轉真空管、高壓終端籠、晶圓靶台磁力抓臂）
     - `diff_furnace.jpg`：橫式高溫熱擴散石英爐管群（四層金黃發熱腔體、晶圓石英舟推入機、質流氣體面板）
     - `tech_cleanroom.jpg`：Q 版無塵衣技術員（兔寶寶裝、護目鏡、口罩、手捧晶圓卡匣盒）
     - `agv_carrier.jpg`：無人搬運自走車 AGV（光學雷達、旋轉警示燈、頂載透光發光 FOUP 晶圓盒）
     - `oht_shuttle.jpg`：天花板空中天軌天車 OHT（磁浮流線型懸掛車體、機械抓爪吊掛 300mm FOUP）
2. **20 款 SVG 佔位檔案徹底向量立體化重構**：
   - 全面替換 `public/assets/machines/` 下所有 20 款原先為「深藍方塊文字框」之簡陋 SVG。
   - 透過 SVG 濾鏡與等角立體投影架構（`<image href="..." filter="url(#iso-shadow)" />`），賦予真實投射陰影（feDropShadow）與 512x512 高清向量容器，使遊戲無論讀取 `.jpg` 或 `.svg` 均呈現 2.5D 等角美術效果！
3. **動態資產索引庫同步更新 (`src/services/AssetRegistry.ts`, `StoreModal.ts`)**：
   - 機台型錄與商城全面指向新生成之獨立 2.5D 高清資產，徹底告別共用貼圖與簡陋線框。
4. **全廠 23 幅資產「外圍邊界 BFS 泛洪去背」轉透明 PNG (Transparent Sprites)**：
   - 採用 Node.js `sharp` 搭配自研**外圍邊界 8 向 BFS 泛洪填充 (Flood Fill) 去背演算法**，從畫布四邊外緣向內收斂清除非機台背景，並進行邊緣抗鋸齒羽化（Anti-Aliasing Feathering）。
   - **關鍵保護機制**：純外部收斂確保機台與人物內部的白色物件（如兔裝無塵衣的白布、白色機身面板、白色儀表字樣）**100% 完整保留，絕不破洞穿透**！
   - 全廠 20 款機台、Q 版無塵衣技術員、AGV 自走車與 OHT 空中天軌天車，全數產出高品質透明底 `.png`。
   - `AssetRegistry.ts`、`StoreModal.ts` 與 `CleanroomScene.ts` 全面升級加載透明 PNG，機台放置在無塵室格柵地坪與黃光光暈上**完全融合、層級遮蔽排序（Depth Sorting）毫無白邊方框穿幫**！

---

### 四、 2.5D 等角無塵室視圖場景 (`src/scenes/CleanroomScene.ts`)
1. **等角座標渲染系統**：
   - 採用標準 2.5D 等角座標公式（`tileWidth: 140, tileHeight: 70`）。
   - 繪製無塵室防靜電地坪格柵與微影黃光區（Yellow Room）琥珀色光暈。
2. **機台與圖層深度排序 (Depth Sorting)**：
   - 機台依據等角座標動態計算 `depth`，確保遮蔽關係精準。
   - 機台正上方配備即時狀態 LED（閒置/加工中/維修/炸毀）與 🛡️ TPM 在線金牌徽章。
3. **動態巡檢與搬運穿梭**：
   - 無塵衣技術人員在機台間走動巡檢。
   - 天花板 OHT (Overhead Hoist Transport) 天軌天車攜帶 FOUP 晶圓盒平滑滑行穿梭。
   - 支援滑鼠拖曳平移與滾輪無段平滑縮放 (0.5x ~ 2.5x)。

---

### 五、 頂層 DOM 科幻玻璃擬態介面群 (`src/ui/`)
1. **`TopHUD.ts` (頂部狀態欄)**：
   - 展示 CEO 頭像、公司名稱、現金、商譽、RollingYieldIndex、工廠負荷條、MES 自動派工開關、靜音按鈕。
2. **`FoundrySetupModal.ts` (創立開場儀式)**：
   - 創角流程：自訂公司名稱（附 🎲 隨機擲骰按鈕）、CEO 姓名、6 款領袖頭像選取。
3. **`AdvisoryModal.ts` (智能瓶頸診斷面板)**：
   - 當工廠負荷超載（>85%）時反推機台產能瓶頸（尤其是 Track 塗膠顯影卡點），提供一鍵跳轉商城/人資對策。
4. **`QuestModal.ts` (每日/每週任務看板)**：
   - 自適應 3 項每日任務、進度條、3/3 全勤特獎領取與每週大獎追蹤。
5. **`AchievementModal.ts` (成就里程碑)**：
   - 16 項成就列表，支援現金領取與【📌 釘選至主畫面】。
6. **`DevConsole.ts` (開發者除錯控制台)**：
   - 快捷鍵 `Ctrl+Shift+D` 或 `?dev=true` 呼出，提供 1x/2x/4x/8x 倍速切換、資金提領、機台磨損觸發與存檔重置。
7. **`ContractModal.ts` (合約看板與在製訂單追蹤)**：
   - 市場 6 張客戶訂單瀏覽、光學極限 CD 防呆、接單即刻領取 NRE 光罩款。
   - 在製晶圓盒加工站點進度、Q-Time 倒數與出貨尾款結算。
8. **`StoreModal.ts` (設備採購商城與現役機台)**：
   - 6 大類設備瀏覽（特別將 **Track 塗膠顯影機獨立分頁**並提醒並聯突破微影瓶頸）。
   - 每款機台配備 **ℹ️ 原理解析** 科普彈窗。
   - 廠內機台資產列表，支援就地大修保養（Overhaul）與報廢變賣。
9. **`HRModal.ts` (人才招募與廠務人資中心)**：
   - 四大職級工程師簽約聘任與機台進駐指派。
   - **【兩班制 (節省 33% 薪資但疲勞高) vs 三班制 (解鎖 🛡️ TPM 零故障)】** 體制切換。
10. **`UIManager.ts` (DOM 介面總協調器)**：
    - 統籌管理所有彈窗開關、鍵盤 ESC 關閉、LocalStorage 存檔以及 JSON 匯出/匯入。

### 六、 次階段核心互動介面全數實作與串接 (Phase 2B UI Modals & Interaction)
1. **`MachinePanel.ts` (單機深度控制面板)**：
   - 點擊場景中任意機台即可直接呼出。
   - 呈現機台即時磨損進度條、健康狀態、就地大修（Overhaul 歸零磨損）與報廢除役變賣。
   - 實時動態 $k_1$ 製程因子分解：`Base(科技) + 磨損 - 調校 + 疲勞`，即時反推有效極限線寬 CD。
   - **Option B 機制核心：微影機專屬「配套 Track 塗膠顯影並聯綁定」清單**，即時對比微影與塗膠顯影總產能，直觀破除黃光瓶頸！
   - 駐機工程師即時指派選單、越級操作（$\ge 2$ 級）25% 炸機風險紅字警告與 🛡️ TPM 在線保養檢核。
2. **`WaferMapModal.ts` (蒙地卡羅 25 晶粒良率晶圓圖檢視器)**：
   - 點擊頂部狀態欄「品質良率 🔍」或合約板在製批次「🔍 晶圓圖」按鈕即可呼出。
   - 圓形矽晶圓盤（Wafer Disc）帶彩虹光澤倒角切口（Bottom Notch）。
   - 5x5 晶粒陣列精準呈現蒙地卡羅同心圓良率梯度（中心 1.1x / 邊緣 0.7x）與缺陷群聚擴散。
   - 點擊任意晶粒即時顯示座標、中心距離、通過狀態與缺陷分類（`OPTICAL_DEFOCUS` 散焦 / `PARTICLE` 落塵 / `CLUSTER` 群聚）。
   - 附帶【🎲 重新模擬抽樣】按鈕，可即時進行蒙地卡羅良率重抽。
3. **`LayerAllocationModal.ts` (先進多層混合微影分層指派介面)**：
   - 針對多層先進晶片訂單（$layerCount > 1$），在合約板提供「🎛️ 微影分層配方」專屬入口。
   - 視覺化區分底層關鍵閘極（FEOL Critical Gate）與上層金屬互連導線/銲墊（BEOL Metal Interconnect / Pad）。
   - 逐層下拉指派微影機台，即時連動 `RayleighEngine` 進行光學解析度合規檢核；若機台解析度不足（$CD > TargetCD$）則醒目紅字警告防呆，並鎖定儲存按鈕。
   - **【⚡ 一鍵最佳經濟配置 (Auto-Fill)】**：自動為關鍵層配置最先進曝光機，非關鍵層自動選用成熟經濟機台，大幅降低高達 60% 的機台折舊與磨損！
4. **`TutorialOverlay.ts` (手把手互動新手教學引導層)**：
   - 5 大引導步驟：創辦人歡迎、合約簽署與 NRE 入帳、產線加工、晶圓良率檢驗、出貨結算與 MES 自動化。
   - 醒目高亮之**【⏩ 免費跳過生產等待】專屬按鈕**：點擊後瞬間完成加工結算良率，讓新玩家體驗直通完工的爽快感，完全免去等待！
   - 隨時可跳過教學，頂部狀態欄 `❓` 按鈕可隨時重新呼出重溫。
5. **全系統事件串接與編譯檢核**：
   - `main.ts`、`TopHUD.ts`、`ContractModal.ts`、`UIManager.ts` 全面互聯。
   - 經由 `npx tsc --noEmit` 靜態型別編譯檢驗，**全專案 0 錯誤通過**。

### 七、 全自動物料搬送系統 (AMHS) 實作、2.5D 載具動態渲染與 CSS 排版優化 (Phase 3 AMHS & Polish)
1. **`AMHSEngine.ts` (廠務全自動物料搬送系統調度引擎)**：
   - 實作 2D 網格 **A* (A-Star) 尋路演算法**，自動避開現有機台佔位格柵，計算最短合法動線。
   - 搬運載具階層規格與速度梯隊：
     - `MANUAL`：無塵衣技術員手持卡匣（1.0 格/秒，容量 1 盒）。
     - `AGV`：地面光學導引無人自走車（2.5 格/秒，容量 2 盒）。
     - `OHT`：天花板磁浮空中天車（5.0 格/秒，容量 1 盒）。
   - **SHR (Super Hot Run) 超急件專屬搶佔協定**：當 SHR 天車高速接近時，附近 2 格內的常規天車即刻切換為 `YIELDING_SHR` 停靠側線避讓！
   - 站點間晶圓傳送秒數精準預估與 Q-Time 逾期風險評級（LOW / MEDIUM / HIGH）。
2. **`CleanroomScene.ts` 載具全面升級 2.5D 動態穿梭**：
   - 預載入人物與搬運載具 Sprite（`tech_cleanroom.png`, `agv_carrier.png`, `oht_shuttle.png`）。
   - 技術員與 OHT 天車全面裝配 2.5D 去背透明立體 Sprite，告別純色塊幾何體。
   - **新增地面 AGV 無人搬運自走車隊**：解鎖 `features.agv` 後自動進駐潔淨室走道，具備光學雷達與 FOUP 載運外觀，在機台間靈活穿梭巡檢。
3. **`style.css` 專項排版與視覺打磨 (Addressing Layout Feedback)**：
   - 彈窗容器升級為 `display: flex; flex-direction: column`，標題列與底部按鈕列常駐鎖定，中段自適應內部滾動，徹底解決小螢幕筆電標題被捲走與底部超出視窗之問題。
   - 升級為精美 5px 青藍色冷光漸層微型捲動條，避免寬滾動條遮擋晶圓圖與合約字樣。
   - 毛玻璃背景模糊度升級至 10px，彈窗陰影增添青藍電漿微光暈（`rgba(6, 182, 212, 0.15)`），整體視覺層次大幅提升。

### 八、 靜態打包建置、全世代數值平衡長時模擬與正式竣工 (Phase 4 Final Delivery)
1. **靜態打包環境收尾 (Vite Production Build & Static Assets)**：
   - 修正 `vite.config.ts` 中的編譯輸出路徑：設定 `assetsDir: 'static'` 與 `emptyOutDir: false`，徹底解決 Windows 作業系統下 `public/assets` 與打包目錄之檔案複製鎖定衝突。
   - 調整 `index.html` 之 `<script src="./phaser.min.js"></script>` 為純相對路徑引用，確保任何子目錄或 GitHub Pages 靜態站點皆能無縫直啟。
   - 執行 `npm run build`（`tsc && vite build`），以 **383 毫秒超高速編譯完畢**，產出健全完整之純靜態 `dist/` 部署包。
2. **全世代數值平衡與通關長時模擬驗證 (`scripts/verify_full_progression.ts`)**：
   - 撰寫端到端自動化驗證腳本，全面模擬完整代工廠運營生命週期：
     - **Tier 1 (10µm 接觸式微影)**：簽署經典訂單、秒收 200 萬 NRE 光罩預付款、批次 5 站流動、95% 良率出貨入帳 150 萬尾款。
     - **TPM 在線預防保養檢核**：磨損度成功鎖定在 0%（合乎 TPM $\le 5\%$ 零故障保證）。
     - **AMHS A* 網格尋路**：成功避開機台佔位計算 9 格最佳路徑。
     - **Tier 3 (CMP 平坦化)**：解鎖化學機械研磨與混合微影多層配方。
     - **Tier 5 (ArFi 浸潤式 + SAQP 多重曝光)**：極限線寬成功壓榨至 41nm。
     - **Tier 6 (High-NA EUV 終極通關)**：累積 100 億資金與連續 5 筆 96%+ 滾動良率，成功觸發解鎖【🏆 稱霸矽島 (Silicon Hegemony)】終極成就！
3. **專案 100% 圓滿竣工**：
   - 全專案 9 大科學引擎、23 款透明機台與載具貼圖、Phaser 3 潔淨室場景、全套 13 個 DOM 科幻介面全部通過型別檢核與生產環境建置。

### 九、 GitHub Pages 上線發布包整理與部署自動化 (GitHub Pages Ready)
1. **`/docs` 靜態發布目錄自動化同步**：
   - 撰寫 `scripts/copy_to_docs.js` 並整合入 `npm run build`，編譯完成後自動將 `dist/` 最新產物（HTML、CSS、JS、phaser.min.js、2.5D 圖片素材）即時鏡像至 `docs/`。
   - 自動寫入 `docs/.nojekyll`，防止 GitHub Pages 的 Jekyll 引擎誤過濾底層靜態資源。
2. **內建 GitHub Actions CI/CD 工作流 (`.github/workflows/deploy.yml`)**：
   - 提供現代化自動化部署流程，使用者只要 `git push`，GitHub 即會自動觸發測試與 Pages 線上發布。
3. **專案規格說明與 Git 倉庫初始化**：
   - 建立完整中文說明文件 [`README.md`](file:///c:/Users/user/Desktop/程式設計/半導體工廠遊戲/README.md)，詳載 1 分鐘極速發布指南與半導體特色機制。
   - 建立 [`.gitignore`](file:///c:/Users/user/Desktop/程式設計/半導體工廠遊戲/.gitignore) 排除龐大 `node_modules/`，並執行 `git init` 完成本地倉庫初始化。

---

### 十、 遠端倉庫推送與正式發布上線 (Official Release & Remote Push)
1. **GitHub 遠端儲存庫成功同步**：
   - 遠端倉庫地址：`https://github.com/chengxray/silicon-tycoon`
   - 分支：`main`
   - Commit 紀錄：`2ce4424` (`feat: Silicon Tycoon (矽島霸權) v1.0.0 full game release with GitHub Pages support`)
   - 完整交付所有原始碼、9 大科學模擬引擎、2.5D 高清去背立體美術資產、Phaser 3 動態無塵室場景、13 個科技感互動介面、以及預建置之 `/docs` 與 `.github/workflows/deploy.yml`。
2. **GitHub Pages 隨開即玩就緒**：
   - 靜態網址：`https://chengxray.github.io/silicon-tycoon/`
   - 提供雙軌部署支援：可直接使用 `/docs` 資料夾零等待發布，或使用 GitHub Actions CI/CD 自動構建。

---

### 十二、 實機操控體驗深度調校與四大核心系統升級 (v1.1.0 Feature Release)

針對玩家反饋之 4 項深度操作與流程系統建議，研發團隊進行了全面的底層重構與體驗升級：

#### 1. 主畫面滑鼠操控與視角拖曳深度調校 (Mouse Sensitivity & Drag Decoupling)
- **問題根因**：原先視角平移無位移閾值判定，滑鼠微動即判定為拖曳，導致點擊機台與地圖拖曳衝突；且當滑鼠拖曳至 Canvas 畫布外或 DOM 視窗上方放開時，Phaser 無法捕捉 `pointerup`，造成 `isDragging` 永久鎖死在啟用狀態。
- **解決對策**：
  - 在 `CleanroomScene.ts` 加入累計拖曳位移量 `totalDragDistance` 與點擊判定門檻 `dragThreshold = 6`。
  - 將機台點擊與地圖拖曳徹底解耦：機台點擊事件改於 `pointerup` 階段觸發，且嚴格要求 `totalDragDistance <= dragThreshold`，徹底消除點擊下層機台或拖曳誤觸的問題。
  - 增加全域 `window.addEventListener('mouseup')` 與 `window.addEventListener('blur')` 監聽，無論滑鼠在哪裡放開或切換視窗，均自動重置拖曳狀態，徹底根除滑鼠黏滯問題。
  - 加入阻尼係數 `cameraPanVelocity *= 0.85`，視角平移靈敏平穩，不再過度敏感晃動。

#### 2. 主畫面機台 2.5D 精美圖片顯示修復 (Dynamic Machine Sprite Loader & Anchor Calibration)
- **問題根因**：原先在 `CleanroomScene` 預加載階段，部分未完成解碼之紋理會導致機台暫時回退至等角幾何方塊 (`entry.fallback`)；且原先包含無效的 SVG 人物幀佔位路徑，阻礙了紋理資源的順利解碼。
- **解決對策**：
  - 清理 `AssetRegistry.ts` 中的佔位 SVG 幀路徑，確保 Phaser 資源佇列 100% 正確解析。
  - 在 `CleanroomScene.ts` 註冊 `this.load.on('complete', () => this.refreshMachineSprites())` 生命週期監聽器，當非同步圖片解碼完成後，自動將場景中暫存的立方體升級為高品質 2.5D PNG 機台貼圖。
  - 精確校準機台 Sprite 縮放寬度為 `tileWidth * 0.95` (`~133px`) 與錨點 `setOrigin(0.5, 0.7)`，使機台穩固坐落於等角菱形防靜電地坪之上，並具備流暢的游標 Hover 提亮高光效果。

#### 3. 全局常駐訂單狀態欄與全機台製程流程看板 (Floating Order Status Bar & Process Pipeline)
- **問題根因**：原先主畫面缺乏直觀的訂單接單提示與即時生產流向回饋，玩家接單後不易掌握訂單顆數進度、目前所在機台以及後續工步流程。
- **解決對策**：
  - **產線待命中指引橫幅**：當無在製訂單時，以醒目的金黃色科幻橫幅顯示「【產線待命中】尚未承接晶圓代工訂單」，配備高亮動態呼吸按鈕 **`🚀 立即承接訂單 (Open Contracts)`**，點擊直達客戶合約公告板。
  - **在製訂單即時多維度儀表板**：接單後即刻展示：
    - 客戶名稱、製程工藝奈米節點 (`nm`)、晶圓當前加工層數 (`第 X / Y 層`)。
    - 顆數交期進度 (`已交付 / 總合約顆數`) 與即時漸層百分比進度條。
    - 當前正在加工之機台名稱 (`📍 ASML Twinscan EXE` 等) 與機台運轉狀態。
    - **黃金微影 Q-Time 實時秒數倒數警報**（秒數 $\le 15$ 秒時自動觸發紅色呼吸告警）。
  - **全機台流水線流程圖 (Interactive Machine Pipeline)**：
    - 完整視覺化 6~7 大關鍵製程節點：`薄膜沉積 FILM ➔ 光阻塗膠 TRACK ➔ 微影曝光 LITHO ➔ 顯影烘烤 DEVELOP ➔ 電漿蝕刻 ETCH ➔ 高溫擴散 DIFF [➔ 平坦化研磨 CMP]`。
    - 各工步具備獨立狀態指示：已完成工步顯示綠色 **`✓ 完工`**；當前工步以青藍流光邊框閃爍 **`⚡ 加工中`**；未達工步標示 `待加工`；若站點機台發生事故則即刻標示 **`💥故障`**。
    - 支援一鍵點擊卡片開啟訂單工單詳細資料或完工出貨結算。

#### 4. 人資系統英文化、全員花名冊與手動排班表 (HR System English Roster & Shift Scheduling)
- **問題根因**：原有人資姓名未標準化；缺乏手動三班制規劃與員工休假疲勞動態機制。
- **解決對策**：
  - **常見英文全名生成庫**：引入包含 Alex Miller, Sarah Chen, Kevin Smith, Emily Williams 等 24 組常用名與姓氏的英文化姓名庫。
  - **舊存檔自動平滑遷移**：在 `SaveGameService` 實裝 `normalizeStaffData()`，讀取舊有存檔時自動將中文或無班別之工程師遷移為英文命名與預設班別。
  - **人資中心三大分頁架構**：
    - `🧑‍🔬 全部員工列表 (All Staff)`：展示職級、專長模組、月薪、疲勞度與進駐機台指派。
    - `📅 廠務排班表 (Shift Schedule)`：全體員工手動班表管理中心。
    - `🤝 人才招募市場 (Hiring Market)`：依 Foundry Tier 招募英文化半導體人才。
  - **手動四班制調度系統**：
    - 提供 **`☀️ 早班 DAY (07-15)`**、**`🌆 中班 SWING (15-23)`**、**`🌙 夜班 NIGHT (23-07)`**、**`🏖️ 排休 OFF`** 獨立切換按鈕。
    - 實裝 **24H 輪班健康度與 TPM 覆蓋看板**：早/中/夜班均有人員駐守時亮起綠色盾牌，解鎖 TPM 24H 零故障在線維護保證！
    - 提供 3 款一鍵智慧排班輔助工具：
      - `🔄 一鍵均衡三班制`：將全體員工平均輪替分配至早班、中班與夜班。
      - `☀️ 一鍵集中早班`：全員集中早班出勤。
      - `🛡️ TPM 最佳化排班`：依疲勞度智慧調度，高疲勞員工強制排休，其餘平均輪值三班。
  - **動態疲勞與休假恢復機制**：
    - 排休 (`OFF`) 之工程師每秒快速恢復疲勞 (`-0.25%/s`)。
    - 出勤工程師正常累積疲勞，夜班疲勞損耗具有 1.5x 加成。
    - 排休員工暫停機台進駐維護，使排班調度具備高度策略性與半導體工廠真實感。

---

---

### 十三、 植物大戰殭屍 1 代風格登入系統與日/周/月多維財務收支報表系統 (v1.2.0 Feature Release)

針對玩家提出之**「加入財報系統（日、周、月收支分析）以及登入系統（植物大戰殭屍 1 代風格）」**需求，研發團隊完成了全面設計與系統落地：

#### 1. 植物大戰殭屍 1 代經典登入系統 (PvZ 1 Style Login / User Profile System)
- **經典「請選擇使用者 (Who are you?)」復古科幻彈窗**：
  - 採用琥珀金屬邊框與深色科技底紋之精美對話框風格（`.pvz-dialog`）。
  - **存檔名冊捲軸清單**：展示每位存檔之玩家姓名、公司抬頭、晶圓廠等級 (`Foundry Tier 1~4`)、手頭現有現金 (`$`)、以及最後登入遊玩時間。
- **全套存檔增刪改查生命週期**：
  - **`➕ 建立新玩家 (Create New User)`**：支援自訂 CEO 姓名與半導體公司名稱，立即生成獨立工廠新進度。
  - **`✏️ 重新命名 (Rename User)`**：隨時編輯目前存檔的 CEO 姓名與企業抬頭。
  - **`🗑️ 刪除存檔 (Delete User)`**：具備二次防呆確認機制，並內建「最後存檔安全保護（禁止刪除唯一存檔）」。
  - **`▶️ 進入遊戲 / 切換存檔 (OK / Play)`**：選取指定存檔後一鍵切換，由 `UIManager` 與 `CleanroomScene` 即時重新載入場景與資料，無需重新整理瀏覽器頁面。
- **全局頂部 HUD 連動**：
  - 頂部導航欄左側展示：`👤 CEO: [玩家名稱] (不是你？點此登入)`，點擊直接快速喚起使用者選擇視窗。
  - 頂部右側快捷工具欄常駐 **`👤 登入`** 與 **`📊 財報`** 兩大功能按鈕。
- **多存檔隔離與平滑遷移 (`SaveGameService.ts`)**：
  - 實作多使用者清單與獨立 LocalStorage 隔離存儲（`SILICON_TYCOON_SAVE_${userId}`）。
  - 自動偵測既有舊存檔，100% 保留現有遊玩進度無損遷移為預設第一位使用者。

#### 2. 半導體代工廠日、周、月多維財務收支分析系統 (Daily, Weekly, Monthly Financial Report System)
- **科學化時間維度與即時記帳引擎 (`src/engine/FinanceEngine.ts`)**：
  - **時間維度換算**：1 遊戲日 (Day) = 30 秒實時運算、1 遊戲周 (Week) = 7 日 (210 秒)、1 遊戲月 (Month) = 28 日 (840 秒)。
  - **全維度營業收入 (Revenues)**：
    - 晶圓完工出貨款 (`waferSales`)
    - 客戶光罩預付款 NRE 款項 (`nreIncome`)
    - 政府與每日任務/成就專項補貼款 (`subsidies`)
  - **營業成本 (COGS) 與營業費用 (OpEx) 分類精算**：
    - 機台折舊費用 (`depreciation`)：依機台購置成本逐秒動態提列。
    - 機台保養與維修費 (`maintenance`)：統計日常 PM 保養與緊急維修花費。
    - 廠務水電與超純水費用 (`utilities`)：依運轉機台數量按日提列。
    - 人力薪資與招募簽約金 (`payroll`)：包含工程師出勤薪資與招募支出。
    - 晶圓報廢與重工損耗 (`scraps`)：光阻重洗與缺陷報廢晶圓之成本計入。
    - 資本支出 (`capex`)：購買全新半導體設備投資款獨立歸戶。
  - **核心財務指標核算**：
    - 營業毛利 (Gross Profit) 與毛利率 (Gross Margin %)。
    - 🎯 **台積電 53% 標竿毛利率指標 (TSMC 53% Benchmark Badge)**：高於 53% 自動亮起「卓越晶圓龍頭」標竿徽章；低於 30% 亮起警訊。
    - 營業淨利 (Net Profit) 與淨利率 (Net Margin %)。
    - 期末現金餘額 (Ending Cash) 與公司總資產淨值 (Company Net Worth)。
- **專業財務報表分析介面 (`src/ui/FinancialReportModal.ts`)**：
  - 提供 **`📅 日收支分析 (Daily)`**、**`📊 周收支分析 (Weekly)`**、**`📈 月收支分析 (Monthly)`** 3 大分頁切換。
  - **4 大 KPI 營運概覽卡片**：即時展示營業總收入、營業總支出、本期營業淨利、以及公司總資產淨值。
  - **收入與支出分類細項損益表 (Income Statement Table)**：清晰列出所有收入與支出項目的金額與占比。
  - **歷史走勢長條圖看板 (Historical Trend Charts)**：
    - 視覺化展示近 14 天 (日報)、近 8 周 (周報)、近 6 個月 (月報) 之每日/周/月淨利走勢。
    - 獲利長條以青藍翠綠漸層高亮，虧損以警示紅標註，游標 Hover 即時浮現詳細收支資訊。
  - **財務長 (CFO) 智慧營運診斷與戰略建議 (CFO Executive Advisory)**：
    - 依據毛利率與淨利動態給予 `EXCELLENT (卓越龍頭)`、`GOOD (營運穩健)`、`WARNING (毛利預警)`、`CRITICAL (財務警戒)` 評級。
    - 產出針對機台稼動率、水電成本管控、先進製程轉移或高毛利合約承接的專業財務改善建議。

---

### 十四、 自定義廠房規劃、黃光微影專區與真實製程物理流速升級（2026-09-08 / v1.3.0）

#### 1. 自訂廠房佈局與黃光專區 (Yellow Room Custom Planner)
- **黃光區劃設機制 (`FacilityState.yellowRoomTiles`)**：
  - 在廠房資料結構中引入 `yellowRoomTiles?: { x: number; y: number }[]` 動態網格陣列，預設無塵室設置標準黃光專區（第 3~7 欄，第 1~3 列）。
  - 無塵室等角投影地面 (`CleanroomScene.renderFloor`) 動態渲染琥珀金黃色地磚 (`0xd97706` / 邊框 `0xf59e0b`)，並自帶黃光室柔和濾光微光與網格標線。
- **機台拖曳搬移與地磚繪製工具列 (`UIManager.renderPlannerToolbar`)**：
  - 頂部導覽列常駐 **`🏗️ 廠房規劃`** 快捷鈕，點擊展開直覺且高科技的規劃工具欄：
    - **🟡 劃設黃光區 (Paint Yellow Room)**：滑鼠點擊地磚即刻噴塗為琥珀黃光防護專區。
    - **⚪ 還原潔淨室 (Paint White Room)**：點選地磚復原為一般白光潔淨室地磚。
    - **🔄 搬移機台 (Move Machine)**：點選機台即呈現旋轉脈衝霓虹光環 (`selectionRingGraphics`)，隨後點擊目標空地磚即刻遷移機台並自動重新規劃 OHT 天車天軌與 AGV 地面巡檢路徑。
    - **💾 完成規劃 (Done)**：立即退出規劃模式並觸發即時自動存檔。

#### 2. 微影機黃光防護與環境白光污染良率歸零機制 (Yellow Room Yield Penalty)
- **半導體光化學原理落地 (`ProductionEngine.advanceLotStation`)**：
  - 微影光阻 (Photoresist) 內含重氮萘醌 (DNQ) 或化學增幅型感光劑 (PAG)，對環境波長小於 500nm 之藍綠白光具有極高感度。
  - 當晶圓批次流轉至微影掃描機 (`LITHO` 曝光 EXPOSE) 或塗膠顯影機 (`TRACK` 塗膠 COAT、顯影 DEVELOP) 時，即時檢驗該機台是否座落於 `yellowRoomTiles` 黃光區網格內。
  - **嚴格懲罰**：若機台未處於黃光專區，環境白光將造成整片晶圓全面非預期感光曝光，批次良率立即**強制歸零 (`lot.yieldMultiplier = 0.0`)**，並標記 `lot.hasYellowRoomViolation = true`，交付顆數直接歸零！
- **多維告警視覺化通知**：
  - **無塵室機台本體標籤**：缺乏黃光防護的 Scanner 或 Track 機台頭頂常駐閃爍紅牌 `🚨 缺乏黃光防護 (良率 0%)`。
  - **機台詳情面板 (`MachinePanel.ts`)**：醒目紅底警報卡說明光阻分子光化學敏感機制，提示玩家透過廠房規劃移動機台。
  - **在製訂單狀態欄 (`OrderStatusWidget`)**：即時告警 `🚨 致命白光污染！微影設備未在黃光區 (良率 0%)`。
  - **晶圓良率檢測圖 (`WaferMapModal.ts`)**：全晶粒標註為失效報廢，遙測根因顯示「【致命白光曝曬污染】微影設備未設置於黃光專區，環境可見光破壞感光光阻化學鍵，全片晶粒完全報廢！」。

#### 3. 擴大地磚格線尺寸（200x100）徹底杜絕前後機台遮蔽
- **幾何尺寸大幅升級**：
  - 等角投影基底格子尺寸由原先的 140x70 擴大至 **200x100**，長寬比例保持標準 2:1 等角透視。
  - 機台 2.5D 精美 Sprite 與幾何佔位尺寸精確微調為 118px，讓前後機台之間擁有充足的無塵走道間隔，前排機台不再遮擋後排機台的操作指示、LED 燈號與工程師頭銜。
  - 預設廠房網格尺寸升級為 10x10，提供寬廣舒適的設備擺放空間與動線餘裕。

#### 4. 徹底修復滑鼠點擊穿透與誤觸下層問題 (Click Penetration Fix)
- **Phaser 容器互動幾何中心校正**：
  - 查明原先使用 `container.setSize(w, h)` 時，Phaser 預設將互動 Hit Area 左上角對齊容器原點 `(0, 0)`，導致點擊判定區嚴重偏向下層前排機台的幾何深層。
  - 改用顯式幾何物件：`new Phaser.Geom.Rectangle(-hitW / 2, -hitH + 15, hitW, hitH)`，使點擊感應區完美居中貼合機台本體。
  - 引入 `pointerDownMachineId` 點擊防呆追蹤機制，僅當滑鼠按下與抬起皆在同一個機台、且拖曳平移距離未超過閾值時才判定為點選，並於各事件回呼嚴格調用 `_pointer.event.stopPropagation()`。
- **DOM UI 層級事件阻擋與穿透修復**：
  - 將 `#hud-widgets`、`#modal-container`、`#tutorial-overlay`、`#dev-console` 頂層容器改為 `pointer-events-none`，杜絕看不見的空白遮罩攔截滑鼠畫布互動。
  - 所有浮動對話框、控制卡片與操作按鈕皆設定 `pointer-events-auto` 並加上 `e.stopPropagation()`，徹底根除點擊穿透至下層畫布或底層按鈕的現象。

#### 5. 生產節奏放慢與真實半導體各站點物理/化學加工時間差異化 (Process Pacing)
- **現實物理與化學反應加工時間精準建模 (`ProductionEngine.BASE_STATION_DURATION_SEC`)**：
  - 原先每秒自動前進一站點的極速節奏已全面重構為基於累積加工秒數的真實進度推進：
    - **DIFF (高溫擴散氧化爐)**：`16 遊戲秒`（高溫化學擴散與熱氧化生成需要長時間恆溫）。
    - **LITHO EXPOSE (微影步進曝光機)**：`12 遊戲秒`（精密微影逐片步進對準與深紫外線曝光）。
    - **ETCH (電漿反應離子蝕刻機 RIE)**：`12 遊戲秒`（電漿離子各向異性物理/化學刻蝕圖形）。
    - **FILM (化學氣相沉積機 CVD/PVD)**：`10 遊戲秒`（原子層薄膜緩慢沉積生長）。
    - **CMP (化學機械平坦化拋光機)**：`10 遊戲秒`（化學研磨漿液精密拋光平整表面）。
    - **TRACK DEVELOP (塗膠顯影機 - 顯影烘烤)**：`8 遊戲秒`（鹼性顯影液洗滌與硬烘烤）。
    - **TRACK COAT (塗膠顯影機 - 旋塗光阻)**：`7 遊戲秒`（高轉速均勻旋轉塗布與軟烘烤）。
- **訂單看板站點即時進度條與倒數計時**：
  - 在製訂單狀態欄 (`OrderStatusWidget`) 即時顯示站點累積進度 `⏱️ 站點進度: Xs / Ys` 與當前站點流水線高亮。
  - 節奏放慢後，玩家能清楚觀察晶圓在各機台之間的流轉加工與 OHT 搬運動畫，大幅增強模擬經營的沉浸感與真實感。

---

### 六、 視覺化製程科技樹、產線待命良率凍結、現實日曆同步與機台搬移修復 (v1.4.0 Milestone)

#### 1. 視覺化半導體製程世代科技樹 (`src/engine/TechTreeEngine.ts` & `src/ui/TechTreeModal.ts`)
- **6 大半導體演進世代天梯 (Tier 1 ~ Tier 6)**：
  - **Tier 1 (微米微影啟蒙時代 5µm ~ 3µm)**：接觸式與等倍投影曝光，汞燈紫外混光，Class 10,000。
  - **Tier 2 (亞微米步進縮小時代 1.2µm ~ 800nm)**：G-line 436nm 步進微影機，解鎖 PECVD 與 RIE 蝕刻。晉升門檻：3 筆訂單、100 片晶圓、NT$ 1,500 萬研發注資。
  - **Tier 3 (次微米紫外深耕時代 500nm ~ 350nm)**：I-line 365nm 步進機，雙軌 Track，解鎖化學機械研磨 (CMP) 與垂直高溫擴散爐。晉升門檻：8 筆訂單、350 片晶圓、NT$ 5,000 萬研發注資。
  - **Tier 4 (深紫外 DUV 跨越時代 250nm ~ 90nm)**：KrF 248nm / ArF 193nm 準分子雷射動態掃描微影，解鎖 MES 智慧派工自動化與原子層沉積 (ALD)。晉升門檻：18 筆訂單、1,000 片晶圓、NT$ 1.8 億研發注資。
  - **Tier 5 (浸潤式微影顛峰時代 65nm ~ 7nm)**：林本堅博士超純水浸潤微影 (NA=1.35)，原子層蝕刻 (ALE) 與多重圖案微影 (SAQP)。晉升門檻：35 筆訂單、3,000 片晶圓、NT$ 6 億研發注資。
  - **Tier 6 (埃米 EUV 矽島霸權時代 5nm ~ 2nm 以下)**：13.5nm 極紫外光 High-NA (0.55 NA) 埃米巨獸，世界半導體工業霸主！晉升門檻：60 筆訂單、8,000 片晶圓、NT$ 25 億研發注資。
- **三維晉升即時指標進度條與互動注資**：
  - 📜 **代工訂單累積交付** (`totalOrdersFulfilled`)：即時追蹤當前完成筆數與目標差額。
  - 💿 **晶圓良品累積量產** (`totalWafersDelivered`)：即時追蹤累計出貨晶圓片數。
  - 💰 **次世代製程研發資金注資** (`rdInvestedCash`)：提供 `+500萬`、`+2000萬`、`+1億` 與 `一鍵全額補足 (Max)` 按鈕，撥款研發資金同步計入財務系統 CapEx。
- **跨模組導航無縫串接**：
  - 頂部導航欄新增 `🔬 研發 (T1~T6)` 按鈕，若達成晉升條件自動閃爍綠色呼吸燈。
  - 設備商城 (`StoreModal.ts`) 中，高階未解鎖機台按鈕轉為 `🔒 需達到 Tier X (前往研發)`，點擊直接連動開啟科技樹。
  - 達成條件後點選「🚀 達成三大研發條件！立即突破晉升！」，即刻晉升 Foundry Tier、重置下一世代注資進度、解鎖新世代機台採購與無塵室 Class，並播放勝利音效。

#### 2. 產線待命良率停止計算 (Idle Yield Freeze)
- **待命良率凍結機制**：
  - 檢測工廠實時在製狀態：`const isProducing = state.activeLots.some(l => l.status === 'PROCESSING') || state.machines.some(m => m.status === 'PROCESSING');`
  - 當產線全數閒置待命（無在製批次或全部批次已完工）時，頂部 HUD 品質良率欄位即時標註 `(待命暫停)`，數字以冷灰文字鎖定，不再進行動態波動或損耗計算。
  - 晶圓良率檢測圖 (`WaferMapModal.ts`) 中同步標示 `⏸️ 產線待命中 (良率暫停)`，讓玩家清晰掌握產線運作與良率計算狀態。

#### 3. 遊戲日與現實日曆完全同步 (Real Calendar Day Sync)
- **現實世界年月日與財務週期同步 (`FinanceEngine.ts`)**：
  - 徹底替換舊有 30 秒一遊戲日的簡化計時器，全面改為依據現實世界真實日曆推進。
  - 記錄真實日期字串 `currentDateStr`（例：`2026-09-08`）、周別字串（例：`2026-W37`）與月份字串（例：`2026-09`）。
  - 當跨越午夜 00:00 真實換日、換周、換月時，自動觸發日報、周報、月報結算與歸檔，保留近 14 天日報、近 8 周周報、近 6 個月月報。
  - 頂部 HUD 與財報中心醒目標註今日現實日期與星期幾：`📅 2026-09-08 (週二) (現實同步)`。

#### 4. 機台搬移成功率極低之根因診斷與 100% 成功率修復 (Machine Relocation Fix)
- **根因分析 (Root-Cause Analysis)**：
  - 查明原先在規劃模式下點擊機台時，Phaser 容器的 `container.on('pointerup')` 先觸發選中機台 (`this.movingMachineId = machine.id`)。
  - 但在同一個放開滑鼠事件瞬間，場景全域 `this.input.on('pointerup')` 亦在相同的螢幕座標觸發，將該點反算為當前格子 `(gx, gy)` 並調用 `handlePlannerTileClick(gx, gy)`。
  - 因 `handlePlannerTileClick` 檢查佔位排除自身，導致系統在 1 毫秒內又把機台放回同一個格子並清空選取 (`this.movingMachineId = null`)，造成機台看似選取失敗！
- **修復方案**：
  - 引入 `justSelectedMachineOnPointerUp` 標記：在點選機台的當幀，場景 `pointerup` 自動略過該次地磚點擊，確保成功進入搬移狀態。
- 在 `handlePlannerTileClick` 增加保護：若點擊格子為機台原先所在位置，不執行任何動作，防止誤放或立刻取消。
  - 實測點選機台顯示高光青藍外框，再點選任意空地磚 100% 準確搬移，徹底解決搬移失敗問題！

---

### 七、 帳號切換無塵室隔離清理、整機資料重置、AMHS 運送設備升級與頂部選單下拉收納優化 (v1.5.0 Milestone)

#### 1. 更換帳號舊機台殘留徹底銷毀與教學模式自動跳出 (Clean Isolation & Auto Tutorial)
- **更換帳號舊機台殘留根因診斷**：
  - 原先 `CleanroomScene.renderMachines` 僅在機台缺少時建立 Container，但切換帳號或更換玩家 ID 時未對已不存在於新存檔的舊機台 Container 進行銷毀。
  - 同時技術員、AGV、OHT 載具在帳號切換時亦未重置，導致上一位玩家的舊機台與人員殘留在畫面上。
- **解決方案**：
  - 在 `CleanroomScene.renderMachines` 中加入動態清理：比較當前 `machineMap` 與存檔機台 ID，將多餘的機台 Container 立即 `destroy()` 並從 Map 移除。
  - 在 `CleanroomScene.updateState` 中檢測 `isUserSwitched`：當偵測到使用者 ID 變更時，徹底清除舊帳號所有機台、技術員、AGV 車與 OHT 天車 Container，重繪乾淨的新帳號無塵室配置。
- **新手教學依玩家帳號獨立記錄與自動跳出**：
  - 在 `PlayerProfile` 中新增 `tutorialCompleted?: boolean` 欄位，存檔中以獨立 key `silicon_tycoon_tutorial_completed_${userId}` 區隔。
  - 在遊戲啟動與切換新帳號時，若該玩家尚未完成教學，系統自動於 400ms 後主動彈出《晶圓製程新手入門指引》，使新創玩家能完整體驗入門流程。

#### 2. 整機資料重置功能與雙重確認視窗 (Factory Reset with Safety Confirmation)
- **徹底清空機制 (`SaveGameService.factoryResetAllData`)**：
  - 自動遍歷並清除瀏覽器 LocalStorage 中所有以 `silicon_tycoon` 或 `SILICON_TYCOON` 開頭的玩家存檔、使用者註冊表與教學完成旗標。
- **高風險雙重確認對話框 (`UIManager.openFactoryResetConfirmation`)**：
  - 在 `UserLoginModal`（PvZ 登入畫面）與頂部 HUD `☰ 更多 ▾` 下拉選單中均提供「💥 整機資料重置」操作。
  - 彈出醒目的紅色警示視窗，明確告知將清除本裝置上的所有存檔、無塵室配置、財務記錄與教學狀態，點擊確認後清空資料並重新整理頁面，提供玩家從零開始體驗遊戲的完整重開機制。

#### 3. 廠務運送設備 (AMHS) 4 階升級系統 (`StoreModal.ts` & `CleanroomScene.ts`)
- **商城新增 AMHS 專屬分類分頁 (`🚚 AMHS 運送設備`)**：
  1. **人工卡匣徒步搬運 (Manual Handling)**：速度 1.0 格/秒，初始掌握，無塵衣技術員手持搬運。
  2. **AGV 磁導激光無人自走車 (Automated Guided Vehicle)**：速度 2.5 格/秒，NT$ 8,000,000，解鎖地面無人自走車自主巡航，降低交接落塵。
  3. **OHT 高速天軌懸吊天車 (Overhead Hoist Transport)**：速度 5.0 格/秒，NT$ 35,000,000（Tier 2+ 解鎖），解鎖天花板閉迴路立體天軌飛行，大幅縮短站間交接耗時。
  4. **SHR 超急件綠波磁浮天車 (Super Hot Run OHT)**：速度 8.0 格/秒（急件 2.5x 提速），NT$ 120,000,000（Tier 4+ 解鎖），空中天車呈現綠波磁浮特效 (Emerald Glow)，最大程度壓制 Q-Time 逾期報廢風險。
- **即時視覺特效升級**：
  - 購買 SHR 升級後，無塵室天車即時提速至 2.5x 並渲染翠綠色科技光暈，無需重啟遊戲。

#### 4. 頂部儀表板 (TopHUD) 排列優化與下拉式選單收納 (Layout Streamlining & Dropdown)
- **排版擁擠與文字垂直擠壓根因**：
  - 原先單列堆疊 13 個按鈕，在常見螢幕寬度下強制壓縮中間 KPI 欄位，因缺乏 `whitespace-nowrap flex-shrink-0`，導致「產 業 商 譽」逐字垂直斷行折行。
- **主次分流重構方案**：
  - 左側（CEO 與公司資訊）與中間（資金、商譽、良率、負荷）加入 `flex-shrink-0 whitespace-nowrap`，堅決杜絕任何文字垂直換行。
  - 右側導航精簡為 **5 大核心高頻按鈕**：
    - `📜 合約`、`🏬 商城`、`🔬 研發 (T1~T6)`、`🏗️ 規劃`、`📊 財報`
  - 其餘次要、輔助與系統功能全數優雅收納於 **`☰ 更多 ▾` 下拉式選單 (Dropdown Menu)**：
    - 👥 人資管理 (HR)
    - 📋 每日任務 (Quests，有未領獎勵時顯示紅/橘呼吸燈)
    - 🏆 產業成就 (Achievements，有可解鎖時顯示綠色徽章)
    - 🤖 MES 自動派工開關 (即時切換 🟢 已開啟 / ⚪ 已停用)
    - 👤 切換玩家帳號 (Who are you?)
    - 💾 存檔與備份 (JSON 匯出/匯入)
### 十三、 植物大戰殭屍 1 代風格登入系統與日/周/月多維財務收支報表系統 (v1.2.0 Feature Release)

針對玩家提出之**「加入財報系統（日、周、月收支分析）以及登入系統（植物大戰殭屍 1 代風格）」**需求，研發團隊完成了全面設計與系統落地：

#### 1. 植物大戰殭屍 1 代經典登入系統 (PvZ 1 Style Login / User Profile System)
- **經典「請選擇使用者 (Who are you?)」復古科幻彈窗**：
  - 採用琥珀金屬邊框與深色科技底紋之精美對話框風格（`.pvz-dialog`）。
  - **存檔名冊捲軸清單**：展示每位存檔之玩家姓名、公司抬頭、晶圓廠等級 (`Foundry Tier 1~4`)、手頭現有現金 (`$`)、以及最後登入遊玩時間。
- **全套存檔增刪改查生命週期**：
  - **`➕ 建立新玩家 (Create New User)`**：支援自訂 CEO 姓名與半導體公司名稱，立即生成獨立工廠新進度。
  - **`✏️ 重新命名 (Rename User)`**：隨時編輯目前存檔的 CEO 姓名與企業抬頭。
  - **`🗑️ 刪除存檔 (Delete User)`**：具備二次防呆確認機制，並內建「最後存檔安全保護（禁止刪除唯一存檔）」。
  - **`▶️ 進入遊戲 / 切換存檔 (OK / Play)`**：選取指定存檔後一鍵切換，由 `UIManager` 與 `CleanroomScene` 即時重新載入場景與資料，無需重新整理瀏覽器頁面。
- **全局頂部 HUD 連動**：
  - 頂部導航欄左側展示：`👤 CEO: [玩家名稱] (不是你？點此登入)`，點擊直接快速喚起使用者選擇視窗。
  - 頂部右側快捷工具欄常駐 **`👤 登入`** 與 **`📊 財報`** 兩大功能按鈕。
- **多存檔隔離與平滑遷移 (`SaveGameService.ts`)**：
  - 實作多使用者清單與獨立 LocalStorage 隔離存儲（`SILICON_TYCOON_SAVE_${userId}`）。
  - 自動偵測既有舊存檔，100% 保留現有遊玩進度無損遷移為預設第一位使用者。

#### 2. 半導體代工廠日、周、月多維財務收支分析系統 (Daily, Weekly, Monthly Financial Report System)
- **科學化時間維度與即時記帳引擎 (`src/engine/FinanceEngine.ts`)**：
  - **時間維度換算**：1 遊戲日 (Day) = 30 秒實時運算、1 遊戲周 (Week) = 7 日 (210 秒)、1 遊戲月 (Month) = 28 日 (840 秒)。
  - **全維度營業收入 (Revenues)**：
    - 晶圓完工出貨款 (`waferSales`)
    - 客戶光罩預付款 NRE 款項 (`nreIncome`)
    - 政府與每日任務/成就專項補貼款 (`subsidies`)
  - **營業成本 (COGS) 與營業費用 (OpEx) 分類精算**：
    - 機台折舊費用 (`depreciation`)：依機台購置成本逐秒動態提列。
    - 機台保養與維修費 (`maintenance`)：統計日常 PM 保養與緊急維修花費。
    - 廠務水電與超純水費用 (`utilities`)：依運轉機台數量按日提列。
    - 人力薪資與招募簽約金 (`payroll`)：包含工程師出勤薪資與招募支出。
    - 晶圓報廢與重工損耗 (`scraps`)：光阻重洗與缺陷報廢晶圓之成本計入。
    - 資本支出 (`capex`)：購買全新半導體設備投資款獨立歸戶。
  - **核心財務指標核算**：
    - 營業毛利 (Gross Profit) 與毛利率 (Gross Margin %)。
    - 🎯 **台積電 53% 標竿毛利率指標 (TSMC 53% Benchmark Badge)**：高於 53% 自動亮起「卓越晶圓龍頭」標竿徽章；低於 30% 亮起警訊。
    - 營業淨利 (Net Profit) 與淨利率 (Net Margin %)。
    - 期末現金餘額 (Ending Cash) 與公司總資產淨值 (Company Net Worth)。
- **專業財務報表分析介面 (`src/ui/FinancialReportModal.ts`)**：
  - 提供 **`📅 日收支分析 (Daily)`**、**`📊 周收支分析 (Weekly)`**、**`📈 月收支分析 (Monthly)`** 3 大分頁切換。
  - **4 大 KPI 營運概覽卡片**：即時展示營業總收入、營業總支出、本期營業淨利、以及公司總資產淨值。
  - **收入與支出分類細項損益表 (Income Statement Table)**：清晰列出所有收入與支出項目的金額與占比。
  - **歷史走勢長條圖看板 (Historical Trend Charts)**：
    - 視覺化展示近 14 天 (日報)、近 8 周 (周報)、近 6 個月 (月報) 之每日/周/月淨利走勢。
    - 獲利長條以青藍翠綠漸層高亮，虧損以警示紅標註，游標 Hover 即時浮現詳細收支資訊。
  - **財務長 (CFO) 智慧營運診斷與戰略建議 (CFO Executive Advisory)**：
    - 依據毛利率與淨利動態給予 `EXCELLENT (卓越龍頭)`、`GOOD (營運穩健)`、`WARNING (毛利預警)`、`CRITICAL (財務警戒)` 評級。
    - 產出針對機台稼動率、水電成本管控、先進製程轉移或高毛利合約承接的專業財務改善建議。

---

### 十四、 自定義廠房規劃、黃光微影專區與真實製程物理流速升級（2026-09-08 / v1.3.0）

#### 1. 自訂廠房佈局與黃光專區 (Yellow Room Custom Planner)
- **黃光區劃設機制 (`FacilityState.yellowRoomTiles`)**：
  - 在廠房資料結構中引入 `yellowRoomTiles?: { x: number; y: number }[]` 動態網格陣列，預設無塵室設置標準黃光專區（第 3~7 欄，第 1~3 列）。
  - 無塵室等角投影地面 (`CleanroomScene.renderFloor`) 動態渲染琥珀金黃色地磚 (`0xd97706` / 邊框 `0xf59e0b`)，並自帶黃光室柔和濾光微光與網格標線。
- **機台拖曳搬移與地磚繪製工具列 (`UIManager.renderPlannerToolbar`)**：
  - 頂部導覽列常駐 **`🏗️ 廠房規劃`** 快捷鈕，點擊展開直覺且高科技的規劃工具欄：
    - **🟡 劃設黃光區 (Paint Yellow Room)**：滑鼠點擊地磚即刻噴塗為琥珀黃光防護專區。
    - **⚪ 還原潔淨室 (Paint White Room)**：點選地磚復原為一般白光潔淨室地磚。
    - **🔄 搬移機台 (Move Machine)**：點選機台即呈現旋轉脈衝霓虹光環 (`selectionRingGraphics`)，隨後點擊目標空地磚即刻遷移機台並自動重新規劃 OHT 天車天軌與 AGV 地面巡檢路徑。
    - **💾 完成規劃 (Done)**：立即退出規劃模式並觸發即時自動存檔。

#### 2. 微影機黃光防護與環境白光污染良率歸零機制 (Yellow Room Yield Penalty)
- **半導體光化學原理落地 (`ProductionEngine.advanceLotStation`)**：
  - 微影光阻 (Photoresist) 內含重氮萘醌 (DNQ) 或化學增幅型感光劑 (PAG)，對環境波長小於 500nm 之藍綠白光具有極高感度。
  - 當晶圓批次流轉至微影掃描機 (`LITHO` 曝光 EXPOSE) 或塗膠顯影機 (`TRACK` 塗膠 COAT、顯影 DEVELOP) 時，即時檢驗該機台是否座落於 `yellowRoomTiles` 黃光區網格內。
  - **嚴格懲罰**：若機台未處於黃光專區，環境白光將造成整片晶圓全面非預期感光曝光，批次良率立即**強制歸零 (`lot.yieldMultiplier = 0.0`)**，並標記 `lot.hasYellowRoomViolation = true`，交付顆數直接歸零！
- **多維告警視覺化通知**：
  - **無塵室機台本體標籤**：缺乏黃光防護的 Scanner 或 Track 機台頭頂常駐閃爍紅牌 `🚨 缺乏黃光防護 (良率 0%)`。
  - **機台詳情面板 (`MachinePanel.ts`)**：醒目紅底警報卡說明光阻分子光化學敏感機制，提示玩家透過廠房規劃移動機台。
  - **在製訂單狀態欄 (`OrderStatusWidget`)**：即時告警 `🚨 致命白光污染！微影設備未在黃光區 (良率 0%)`。
  - **晶圓良率檢測圖 (`WaferMapModal.ts`)**：全晶粒標註為失效報廢，遙測根因顯示「【致命白光曝曬污染】微影設備未設置於黃光專區，環境可見光破壞感光光阻化學鍵，全片晶粒完全報廢！」。

#### 3. 擴大地磚格線尺寸（200x100）徹底杜絕前後機台遮蔽
- **幾何尺寸大幅升級**：
  - 等角投影基底格子尺寸由原先的 140x70 擴大至 **200x100**，長寬比例保持標準 2:1 等角透視。
  - 機台 2.5D 精美 Sprite 與幾何佔位尺寸精確微調為 118px，讓前後機台之間擁有充足的無塵走道間隔，前排機台不再遮擋後排機台的操作指示、LED 燈號與工程師頭銜。
  - 預設廠房網格尺寸升級為 10x10，提供寬廣舒適的設備擺放空間與動線餘裕。

#### 4. 徹底修復滑鼠點擊穿透與誤觸下層問題 (Click Penetration Fix)
- **Phaser 容器互動幾何中心校正**：
  - 查明原先使用 `container.setSize(w, h)` 時，Phaser 預設將互動 Hit Area 左上角對齊容器原點 `(0, 0)`，導致點擊判定區嚴重偏向下層前排機台的幾何深層。
  - 改用顯式幾何物件：`new Phaser.Geom.Rectangle(-hitW / 2, -hitH + 15, hitW, hitH)`，使點擊感應區完美居中貼合機台本體。
  - 引入 `pointerDownMachineId` 點擊防呆追蹤機制，僅當滑鼠按下與抬起皆在同一個機台、且拖曳平移距離未超過閾值時才判定為點選，並於各事件回呼嚴格調用 `_pointer.event.stopPropagation()`。
- **DOM UI 層級事件阻擋與穿透修復**：
  - 將 `#hud-widgets`、`#modal-container`、`#tutorial-overlay`、`#dev-console` 頂層容器改為 `pointer-events-none`，杜絕看不見的空白遮罩攔截滑鼠畫布互動。
  - 所有浮動對話框、控制卡片與操作按鈕皆設定 `pointer-events-auto` 並加上 `e.stopPropagation()`，徹底根除點擊穿透至下層畫布或底層按鈕的現象。

#### 5. 生產節奏放慢與真實半導體各站點物理/化學加工時間差異化 (Process Pacing)
- **現實物理與化學反應加工時間精準建模 (`ProductionEngine.BASE_STATION_DURATION_SEC`)**：
  - 原先每秒自動前進一站點的極速節奏已全面重構為基於累積加工秒數的真實進度推進：
    - **DIFF (高溫擴散氧化爐)**：`16 遊戲秒`（高溫化學擴散與熱氧化生成需要長時間恆溫）。
    - **LITHO EXPOSE (微影步進曝光機)**：`12 遊戲秒`（精密微影逐片步進對準與深紫外線曝光）。
    - **ETCH (電漿反應離子蝕刻機 RIE)**：`12 遊戲秒`（電漿離子各向異性物理/化學刻蝕圖形）。
    - **FILM (化學氣相沉積機 CVD/PVD)**：`10 遊戲秒`（原子層薄膜緩慢沉積生長）。
    - **CMP (化學機械平坦化拋光機)**：`10 遊戲秒`（化學研磨漿液精密拋光平整表面）。
    - **TRACK DEVELOP (塗膠顯影機 - 顯影烘烤)**：`8 遊戲秒`（鹼性顯影液洗滌與硬烘烤）。
    - **TRACK COAT (塗膠顯影機 - 旋塗光阻)**：`7 遊戲秒`（高轉速均勻旋轉塗布與軟烘烤）。
- **訂單看板站點即時進度條與倒數計時**：
  - 在製訂單狀態欄 (`OrderStatusWidget`) 即時顯示站點累積進度 `⏱️ 站點進度: Xs / Ys` 與當前站點流水線高亮。
  - 節奏放慢後，玩家能清楚觀察晶圓在各機台之間的流轉加工與 OHT 搬運動畫，大幅增強模擬經營的沉浸感與真實感。

---

### 六、 視覺化製程科技樹、產線待命良率凍結、現實日曆同步與機台搬移修復 (v1.4.0 Milestone)

#### 1. 視覺化半導體製程世代科技樹 (`src/engine/TechTreeEngine.ts` & `src/ui/TechTreeModal.ts`)
- **6 大半導體演進世代天梯 (Tier 1 ~ Tier 6)**：
  - **Tier 1 (微米微影啟蒙時代 5µm ~ 3µm)**：接觸式與等倍投影曝光，汞燈紫外混光，Class 10,000。
  - **Tier 2 (亞微米步進縮小時代 1.2µm ~ 800nm)**：G-line 436nm 步進微影機，解鎖 PECVD 與 RIE 蝕刻。晉升門檻：3 筆訂單、100 片晶圓、NT$ 1,500 萬研發注資。
  - **Tier 3 (次微米紫外深耕時代 500nm ~ 350nm)**：I-line 365nm 步進機，雙軌 Track，解鎖化學機械研磨 (CMP) 與垂直高溫擴散爐。晉升門檻：8 筆訂單、350 片晶圓、NT$ 5,000 萬研發注資。
  - **Tier 4 (深紫外 DUV 跨越時代 250nm ~ 90nm)**：KrF 248nm / ArF 193nm 準分子雷射動態掃描微影，解鎖 MES 智慧派工自動化與原子層沉積 (ALD)。晉升門檻：18 筆訂單、1,000 片晶圓、NT$ 1.8 億研發注資。
  - **Tier 5 (浸潤式微影顛峰時代 65nm ~ 7nm)**：林本堅博士超純水浸潤微影 (NA=1.35)，原子層蝕刻 (ALE) 與多重圖案微影 (SAQP)。晉升門檻：35 筆訂單、3,000 片晶圓、NT$ 6 億研發注資。
  - **Tier 6 (埃米 EUV 矽島霸權時代 5nm ~ 2nm 以下)**：13.5nm 極紫外光 High-NA (0.55 NA) 埃米巨獸，世界半導體工業霸主！晉升門檻：60 筆訂單、8,000 片晶圓、NT$ 25 億研發注資。
- **三維晉升即時指標進度條與互動注資**：
  - 📜 **代工訂單累積交付** (`totalOrdersFulfilled`)：即時追蹤當前完成筆數與目標差額。
  - 💿 **晶圓良品累積量產** (`totalWafersDelivered`)：即時追蹤累計出貨晶圓片數。
  - 💰 **次世代製程研發資金注資** (`rdInvestedCash`)：提供 `+500萬`、`+2000萬`、`+1億` 與 `一鍵全額補足 (Max)` 按鈕，撥款研發資金同步計入財務系統 CapEx。
- **跨模組導航無縫串接**：
  - 頂部導航欄新增 `🔬 研發 (T1~T6)` 按鈕，若達成晉升條件自動閃爍綠色呼吸燈。
  - 設備商城 (`StoreModal.ts`) 中，高階未解鎖機台按鈕轉為 `🔒 需達到 Tier X (前往研發)`，點擊直接連動開啟科技樹。
  - 達成條件後點選「🚀 達成三大研發條件！立即突破晉升！」，即刻晉升 Foundry Tier、重置下一世代注資進度、解鎖新世代機台採購與無塵室 Class，並播放勝利音效。

#### 2. 產線待命良率停止計算 (Idle Yield Freeze)
- **待命良率凍結機制**：
  - 檢測工廠實時在製狀態：`const isProducing = state.activeLots.some(l => l.status === 'PROCESSING') || state.machines.some(m => m.status === 'PROCESSING');`
  - 當產線全數閒置待命（無在製批次或全部批次已完工）時，頂部 HUD 品質良率欄位即時標註 `(待命暫停)`，數字以冷灰文字鎖定，不再進行動態波動或損耗計算。
  - 晶圓良率檢測圖 (`WaferMapModal.ts`) 中同步標示 `⏸️ 產線待命中 (良率暫停)`，讓玩家清晰掌握產線運作與良率計算狀態。

#### 3. 遊戲日與現實日曆完全同步 (Real Calendar Day Sync)
- **現實世界年月日與財務週期同步 (`FinanceEngine.ts`)**：
  - 徹底替換舊有 30 秒一遊戲日的簡化計時器，全面改為依據現實世界真實日曆推進。
  - 記錄真實日期字串 `currentDateStr`（例：`2026-09-08`）、周別字串（例：`2026-W37`）與月份字串（例：`2026-09`）。
  - 當跨越午夜 00:00 真實換日、換周、換月時，自動觸發日報、周報、月報結算與歸檔，保留近 14 天日報、近 8 周周報、近 6 個月月報。
  - 頂部 HUD 與財報中心醒目標註今日現實日期與星期幾：`📅 2026-09-08 (週二) (現實同步)`。

#### 4. 機台搬移成功率極低之根因診斷與 100% 成功率修復 (Machine Relocation Fix)
- **根因分析 (Root-Cause Analysis)**：
  - 查明原先在規劃模式下點擊機台時，Phaser 容器的 `container.on('pointerup')` 先觸發選中機台 (`this.movingMachineId = machine.id`)。
  - 但在同一個放開滑鼠事件瞬間，場景全域 `this.input.on('pointerup')` 亦在相同的螢幕座標觸發，將該點反算為當前格子 `(gx, gy)` 並調用 `handlePlannerTileClick(gx, gy)`。
  - 因 `handlePlannerTileClick` 檢查佔位排除自身，導致系統在 1 毫秒內又把機台放回同一個格子並清空選取 (`this.movingMachineId = null`)，造成機台看似選取失敗！
- **修復方案**：
  - 引入 `justSelectedMachineOnPointerUp` 標記：在點選機台的當幀，場景 `pointerup` 自動略過該次地磚點擊，確保成功進入搬移狀態。
- 在 `handlePlannerTileClick` 增加保護：若點擊格子為機台原先所在位置，不執行任何動作，防止誤放或立刻取消。
  - 實測點選機台顯示高光青藍外框，再點選任意空地磚 100% 準確搬移，徹底解決搬移失敗問題！

---

### 七、 帳號切換無塵室隔離清理、整機資料重置、AMHS 運送設備升級與頂部選單下拉收納優化 (v1.5.0 Milestone)

#### 1. 更換帳號舊機台殘留徹底銷毀與教學模式自動跳出 (Clean Isolation & Auto Tutorial)
- **更換帳號舊機台殘留根因診斷**：
  - 原先 `CleanroomScene.renderMachines` 僅在機台缺少時建立 Container，但切換帳號或更換玩家 ID 時未對已不存在於新存檔的舊機台 Container 進行銷毀。
  - 同時技術員、AGV、OHT 載具在帳號切換時亦未重置，導致上一位玩家的舊機台與人員殘留在畫面上。
- **解決方案**：
  - In `CleanroomScene.renderMachines` 中加入動態清理：比較當前 `machineMap` 與存檔機台 ID，將多餘的機台 Container 立即 `destroy()` 並從 Map 移除。
  - 在 `CleanroomScene.updateState` 中檢測 `isUserSwitched`：當偵測到使用者 ID 變更時，徹底清除舊帳號所有機台、技術員、AGV 車與 OHT 天車 Container，重繪乾淨的新帳號無塵室配置。
- **新手教學依玩家帳號獨立記錄與自動跳出**：
  - 在 `PlayerProfile` 中新增 `tutorialCompleted?: boolean` 欄位，存檔中以獨立 key `silicon_tycoon_tutorial_completed_${userId}` 區隔。
  - 在遊戲啟動與切換新帳號時，若該玩家尚未完成教學，系統自動於 400ms 後主動彈出《晶圓製程新手入門指引》，使新創玩家能完整體驗入門流程。

#### 2. 整機資料重置功能與雙重確認視窗 (Factory Reset with Safety Confirmation)
- **徹底清空機制 (`SaveGameService.factoryResetAllData`)**：
  - 自動遍歷並清除瀏覽器 LocalStorage 中所有以 `silicon_tycoon` 或 `SILICON_TYCOON` 開頭的玩家存檔、使用者註冊表與教學完成旗標。
- **高風險雙重確認對話框 (`UIManager.openFactoryResetConfirmation`)**：
  - 在 `UserLoginModal`（PvZ 登入畫面）與頂部 HUD `☰ 更多 ▾` 下拉選單中均提供「💥 整機資料重置」操作。
  - 彈出醒目的紅色警示視窗，明確告知將清除本裝置上的所有存檔、無塵室配置、財務記錄與教學狀態，點擊確認後清空資料並重新整理頁面，提供玩家從零開始體驗遊戲的完整重開機制。

#### 3. 廠務運送設備 (AMHS) 4 階升級系統 (`StoreModal.ts` & `CleanroomScene.ts`)
- **商城新增 AMHS 專屬分類分頁 (`🚚 AMHS 運送設備`)**：
  1. **人工卡匣徒步搬運 (Manual Handling)**：速度 1.0 格/秒，初始掌握，無塵衣技術員手持搬運。
  2. **AGV 磁導激光無人自走車 (Automated Guided Vehicle)**：速度 2.5 格/秒，NT$ 8,000,000，解鎖地面無人自走車自主巡航，降低交接落塵。
  3. **OHT 高速天軌懸吊天車 (Overhead Hoist Transport)**：速度 5.0 格/秒，NT$ 35,000,000（Tier 2+ 解鎖），解鎖天花板閉迴路立體天軌飛行，大幅縮短站間交接耗時。
  4. **SHR 超急件綠波磁浮天車 (Super Hot Run OHT)**：速度 8.0 格/秒（急件 2.5x 提速），NT$ 120,000,000（Tier 4+ 解鎖），空中天車呈現綠波磁浮特效 (Emerald Glow)，最大程度壓制 Q-Time 逾期報廢風險。
- **即時視覺特效升級**：
  - 購買 SHR 升級後，無塵室天車即時提速至 2.5x 並渲染翠綠色科技光暈，無需重啟遊戲。

#### 4. 頂部儀表板 (TopHUD) 排列優化與下拉式選單收納 (Layout Streamlining & Dropdown)
- **排版擁擠與文字垂直擠壓根因**：
  - 原先單列堆疊 13 個按鈕，在常見螢幕寬度下強制壓縮中間 KPI 欄位，因缺乏 `whitespace-nowrap flex-shrink-0`，導致「產 業 商 譽」逐字垂直斷行折行。
- **主次分流重構方案**：
  - 左側（CEO 與公司資訊）與中間（資金、商譽、良率、負荷）加入 `flex-shrink-0 whitespace-nowrap`，堅決杜絕任何文字垂直換行。
  - 右側導航精簡為 **5 大核心高頻按鈕**：
    - `📜 合約`、`🏬 商城`、`🔬 研發 (T1~T6)`、`🏗️ 規劃`、`📊 財報`
  - 其餘次要、輔助與系統功能全數優雅收納於 **`☰ 更多 ▾` 下拉式選單 (Dropdown Menu)**：
    - 👥 人資管理 (HR)
    - 📋 每日任務 (Quests，有未領獎勵時顯示紅/橘呼吸燈)
    - 🏆 產業成就 (Achievements，有可解鎖時顯示綠色徽章)
    - 🤖 MES 自動派工開關 (即時切換 🟢 已開啟 / ⚪ 已停用)
    - 👤 切換玩家帳號 (Who are you?)
    - 💾 存檔與備份 (JSON 匯出/匯入)
    - ❓ 晶圓製程教學導引
    - 🔊/🔇 遊戲音效開關
    - 💥 整機資料重置 (Factory Reset)
  - 支援點擊選單外部自動關閉 (Click Outside to Close)，介面清爽大器，各裝置螢幕完美適配！

---

### 八、 下拉式選單穩定性與合約看板經濟重構 (v1.6.0 Dropdown & Contract Engine Upgrade)

#### 1. 下拉式選單防閃退與大字體排版優化 (Dropdown Menu Stability & Typography)
- **「字太小且閃一下就關閉」根本原因診斷**：
  - 原先每秒定時執行的 `simulationTick()` 會呼叫 `topHUD.render()`，每次均重寫 `container.innerHTML`，重新生成帶有 `hidden` 屬性的選單 DOM，使展開的下拉選單在 1 秒內被強制消滅重置，並重播 `animate-fadeIn` 造成視覺高頻閃爍。
  - 同時選單字體設為 `text-xs` (12px) 與 `text-[10px]` (10px)，寬度僅 `w-64` (256px)，在各尺寸螢幕上字體偏小且觸控與滑鼠點擊困難。
  - 每次重繪均向 `document` 重複綁定 `outsideClickListener`，產生事件監聽器累積外洩。
- **徹底修復方案 (`TopHUD.ts`)**：
  - **DOM 結構單次構建 + 數值精準增量更新**：將 HUD 靜態骨架與事件監聽綁定收斂至首次渲染，每秒模擬循環僅更新各欄位之 `.textContent` 與寬度/樣式類別，不再摧毀 DOM 樹，徹底根除選單自動閃退與重新重播動畫問題。
  - **選單開啟狀態狀態機控管 (`isDropdownOpen`)**：選單展開與收合受獨立布林變數維護，點擊外部時統一且唯一監聽，關閉時才加上 `hidden`。
  - **版面大幅加寬與字體升級**：
    - 選單寬度擴展至 `w-80` (320px)。
    - 選單項目文字全面升級為 `text-sm font-semibold` (14px)。
    - 左側圖示放大為 `text-lg`，間距調整為寬鬆舒適之 `px-4 py-2.5`。
    - 狀態標籤與數量升級為 `text-xs font-mono font-medium px-2 py-0.5 rounded`。
    - 頂部導航之「☰ 更多 ▾」按鈕亦放大為 `text-xs sm:text-sm font-bold py-1.5 px-3.5`。

#### 2. 合約訂單經濟天平重磅校正 (Contract Economics & Realistic NRE Rebalancing)
- **「光罩佔整筆訂單超過 90%」數值失衡根因**：
  - 原先 10µm 製程 BasePrice 僅 NT$ 2~6 / 顆，而 BaseNRE 高達 NT$ 80,000 / 層。以 2,500 顆 6 層光罩訂單為例，晶圓出貨款僅 NT$ 9,500，光罩研發款卻高達 NT$ 480,000，光罩佔比超過 98%，導致生產與良率幾乎失去經濟意義。
- **全製程定價表重新校準 (`EconomyEngine.PRICING_TABLE`)**：
  - 依據半導體真實產業經濟，重塑 10µm 至 2nm 共 10 大節點之 BasePrice 與 BaseNRE：
    - **10µm**：BasePrice NT$ 180 / 顆，BaseNRE NT$ 18,000 / 層（晶圓出貨佔比約 84.4%，NRE 佔比約 15.6%）。
    - **5µm**：BasePrice NT$ 320 / 顆，BaseNRE NT$ 38,000 / 層（晶圓出貨約 83.1%，NRE 約 16.9%）。
    - **1µm**：BasePrice NT$ 650 / 顆，BaseNRE NT$ 95,000 / 層。
    - **0.5µm**：BasePrice NT$ 1,200 / 顆，BaseNRE NT$ 220,000 / 層。
    - **350nm**：BasePrice NT$ 2,200 / 顆，BaseNRE NT$ 480,000 / 層。
    - **180nm**：BasePrice NT$ 4,500 / 顆，BaseNRE NT$ 1,100,000 / 層。
    - **90nm**：BasePrice NT$ 8,800 / 顆，BaseNRE NT$ 2,800,000 / 層。
    - **45nm**：BasePrice NT$ 16,000 / 顆，BaseNRE NT$ 7,500,000 / 層。
    - **7nm**：BasePrice NT$ 28,000 / 顆，BaseNRE NT$ 18,000,000 / 層。
    - **2nm**：BasePrice NT$ 45,000 / 顆，BaseNRE NT$ 35,000,000 / 層。
  - 將出貨尾款拉升至訂單總額之 **78% ~ 85%**，NRE 預付款回歸合理的 **15% ~ 22%** 研發補助比例，使工廠良率與量產交付成為決定獲利的絕對關鍵。

#### 3. 合約池持久化、逐筆冷卻補齊與商業獵單付費刷新 (`ContractModal.ts` & `SaveGameV2`)
- **合約看板持久化 (Persistent Contract Board)**：
  - 在 `SaveGameV2` 中新增 `marketOrders: OrderData[]` 與 `nextOrderRespawnTime: number`。
  - 開啟或關閉合約視窗絕不再刷新或重洗合約池，既有訂單完整留存。
- **60 秒冷卻逐筆補齊機制 (Single-Order Replenishment Cooldown)**：
  - 合約看板上限維持 5 筆（`MAX_MARKET_ORDERS = 5`）。
  - 當玩家接下一筆訂單後，合約池餘額扣減，並啟動 60 秒冷卻計時器（`nextOrderRespawnTime = Date.now() + 60,000`）。
  - 合約看板頂部常駐即時倒數橫幅：`⏳ 新客戶合約洽談中... 下一筆合約抵達：XX 秒`。
  - 倒數歸零時精準補充 1 筆新合約，直到補滿 5 筆為止，並在背景 simulationTick 中持續運算。
- **商業獵單付費刷新機制 (Tier-Scaled Paid Sourcing Refresh)**：
  - 徹底移除免費無腦刷新機制。
  - 依據工廠等級收取商業獵頭顧問費：
    - Tier 1: NT$ 50,000
    - Tier 2: NT$ 150,000
    - Tier 3: NT$ 500,000
    - Tier 4: NT$ 1,500,000
    - Tier 5: NT$ 5,000,000
    - Tier 6: NT$ 20,000,000
  - 點擊按鈕彈出雙重確認視窗，核對廠房現金餘額，扣除資金並入帳營業費用 (`FinanceEngine.recordOpEx`)，一次性替換 5 筆全新客戶合約並重置冷卻。

---

## 🏆 專案研發狀態：🎉 v1.6.0 正式交付並通過全線構建 (Shipped to Production)

全系統經完整驗證：TypeScript 靜態型別檢查 0 錯誤、Vite 靜態 Bundle 打包成功、`/docs` 靜態部署分支全數同步完成！下拉選單每秒閃退修復與大字體升級、合約訂單定價真實度天平校準、合約持久化不刷新、接單 60 秒逐筆冷卻補齊、以及等級付費商業獵單刷新全數高品質交付！
